# node-ts-template

Install packages
```shell
yarn install
```

Run kafka
```shell
docker-compose up
```

Create topic
```shell
bash bash/topic.bash 
```

Run producer
```shell
yarn run producer
```

Run consumer
```shell
yarn run consumer
```